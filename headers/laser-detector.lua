---@class LaserDetector : GenericElement
LaserDetector = GenericElement:new()

---Checks if any laser is hitting the detector
---@return boolean hit True if a laser is hitting the detector
function LaserDetector.isHit() end
